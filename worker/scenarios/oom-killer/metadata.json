{
  "id": "oom-killer",
  "name": "OOM Killer Active — Application Being Killed",
  "description": "A 'myapp' process is running on a system with no swap and historical OOM kill events in /var/log/kern.log. The student must set oom_score_adj to protect the application, add swap space, and persist sysctl settings to /etc/sysctl.d/.",
  "compatible_modules": ["labs/linux-ec2"],
  "setup_timeout_seconds": 30,
  "tags": ["memory", "oom", "kernel", "sysctl", "performance", "linux"]
}
